import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { Dev, App } from './app/App';

export default SmarTask = AppRegistry.registerComponent(appName, () => App);
